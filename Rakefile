require'./config/environment'
require "sinatra/activerecord/rake"
#we'll require our config/environment.rb file to 
#load up our environment, as well as "sinatra/activerecord/rake" to get Rake tasks from the sinatra-activerecord gem.
